<div class="center jumbotron">

<h1>Hi 
<% if current_user.role == User.roles.keys[0] %>
 <%=Student.where("email = ?", current_user.email).pluck(:Fname).first%>
<% else %>
Professor <%= current_user.email %>
<% end %>
! Welcome to GoodPET<br>(Good Peer Evaluation Tool)</h1>
<h2>

</h2>
<% if current_user && current_user.admin? %>
    <%= link_to "Manage Teams", '../teams', class: "btn btn-lg btn-primary" %>
    <%= link_to "Manage Projects", '../projects', class: "btn btn-lg btn-primary" %>
    <%= link_to "See the students", '../students', class: "btn btn-lg btn-primary" %>
    <%= link_to "View Reviews", '../reviews', class: "btn btn-lg btn-primary" %>
<% else %>
    <%= link_to "Check your classmates", '../students', class: "btn btn-lg btn-primary" %>
    <%= link_to "Check your teammates", '../teams', class: "btn btn-lg btn-primary" %>
    <%= link_to "See your Projects and Give Reviews", '../projects', class: "btn btn-lg btn-primary" %>
    <%= link_to "View Reviews", '../reviews', class: "btn btn-lg btn-primary" %>
<% end %>
</div>

<%
 if current_user.role == User.roles.keys[0] && !Student.exists?(:email => current_user.email)
        controller.redirect_to '/students/new'
    end
%>